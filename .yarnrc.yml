enableGlobalCache: false

nodeLinker: pnp

# bcrypt's node-pre-gyp install cannot load from Yarn's zip cache
compressionLevel: 0

supportedArchitectures:
  cpu:
    - arm64
    - x64
  libc:
    - glibc
  os:
    - darwin
    - linux
